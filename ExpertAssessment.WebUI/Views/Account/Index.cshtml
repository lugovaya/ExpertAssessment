@{
    ViewBag.Title = "Index";
}

@model ExpertAssessment.Domain.Entities.Expert
<div class="container">
    <h3 style="text-align: center;">Analytic Hierarchy Process </h3>
    <div id="myCarousel" class="carousel slide" style="margin-left: 10%;">
        <!--ol class="carousel-indicators">
    <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
    <li data-target="#myCarousel" data-slide-to="1"></li>
    <li data-target="#myCarousel" data-slide-to="2"></li>
  </!--ol-->
        <!-- Carousel items -->
        <div class="carousel-inner" style="margin-left: 12%;">
            <div class="active item">
                <img src="../../Content/images/glyphicons-halflings.png">
            </div>
            <div class="item">
                <img src="../../Content/images/glyphicons-halflings.png">
            </div>
            <div class="item">
                <img src="../../Content/images/glyphicons-halflings.png">
            </div>
        </div>
        <!-- Carousel nav -->
        <a class="carousel-control left" href="#myCarousel" data-slide="prev">&lsaquo;</a>
        <a class="carousel-control right" href="#myCarousel" data-slide="next">&rsaquo;</a>
    </div>
    
    </div>
@* TODO something more right*@
    
    @if(User.Identity.Name != "")
    {
        @Html.Action("LoginIn", "Account")
    } else
    {
        @Html.Action("Login", "Account")
    }